[project]
name = "eclm-atm-forcing-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cdsapi>=0.7.7",
    "netcdf4>=1.7.4",
    "numpy>=1.25.1",
    "xarray>=2023.8.0",
]

[tool.setuptools]
packages = []
