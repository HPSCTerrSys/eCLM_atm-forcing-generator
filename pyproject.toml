[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "eclm-atm-forcing-generator"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "netCDF4",
]

[tool.setuptools]
packages = []
